import { Text, View } from "react-native";

const Home = ()=> {
    return <View style = {style}>
        <Text>Home Page</Text>
    </View>
}

export default Home;

const style = {
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
}